{
  "name": "kyro-edge",
  "version": "0.0.0",
  "private": true,
  "description": "Kyro edge stream — offline on-device TBI decision co-pilot (E1–E7). Pre-scaffold; this package.json currently exists to pin the seam-critical crypto + a sqlite-vec for the E1 parity harness.",
  "type": "module",
  "scripts": {
    "parity": "node --experimental-sqlite --experimental-strip-types e1/parity.ts",
    "e3": "python e3/conformance.py",
    "e4": "python e4/conformance.py"
  },
  "dependencies": {
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0"
  }
}
